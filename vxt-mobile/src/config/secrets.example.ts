/**
 * Copy this file to secrets.ts and fill in real values.
 * secrets.ts is gitignored — never commit real credentials.
 */
export const IOT_HUB_CONNECTION_STRING =
  'HostName=<hub>.azure-devices.net;DeviceId=<deviceId>;SharedAccessKey=<key>';

export const DEFAULT_USER_ID = '<userId>';
