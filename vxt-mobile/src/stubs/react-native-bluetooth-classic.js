/**
 * Stub for react-native-bluetooth-classic.
 * Used by Metro when the library is not yet installed (npm install react-native-bluetooth-classic).
 * When the real library is installed, Metro will use it from node_modules instead.
 */
module.exports = { default: null };
