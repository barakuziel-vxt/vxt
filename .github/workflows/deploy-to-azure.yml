# DEPRECATED - USE deploy-web-app-to-azure.yml INSTEAD
# This file is being removed and replaced by deploy-web-app-to-azure.yml
# Do not use this workflow - it will be deleted in the next commit

name: DEPRECATED-Deploy-to-Azure-Web-App-DO-NOT-USE

on:
  workflow_dispatch:
    branches:
      - NEVER_TRIGGER
    paths:
      - '.never-match-any-path/*'
